#include <iostream>
#include <string>
#include <vector>
#include <memory>
#include <chrono>

//enums
enum class NotificationChannelType {
    EMAIL,
    SMS,
    IN_APP,
    PUSH
};

enum class MessageType {
    DIRECT,
    GROUP
};


